# ---------------------------------------------------------------------------
# 凭据 —— 绝不能提交到任何仓库（token.txt / JWT 等同账号密码）
# ---------------------------------------------------------------------------
token.txt
*.token
user_name.txt
savings_state.json
# 个性化扫描词表（含用户名/真名）
# ⚠️ gitignore 不支持行内注释 —— 注释必须单独一行，否则整条规则失效
private_patterns.txt

# ---------------------------------------------------------------------------
# 运行时产物 / 本机状态
# ---------------------------------------------------------------------------
hainnu_proxy.log
hainnu_proxy_gui.log
usage_log.jsonl
*.log

# ---------------------------------------------------------------------------
# 环境与缓存（机器专属，体积大；.ruff_cache 内含本机绝对路径，务必排除）
# ---------------------------------------------------------------------------
.venv/
runtime/
chrome-profile/
__pycache__/
.ruff_cache/
*.pyc

# ---------------------------------------------------------------------------
# 备份 / 打包产物 / 临时目录
# ---------------------------------------------------------------------------
*.bak
*.bak-*
*.bak[0-9]*
*.zip
_smoke/
tmp/

# 本地实验目录（测试 / 探测 / 压测 / 笔记 / 截图 / 旧包）
_dev/

# ---------------------------------------------------------------------------
# 开发 / 实验 / 测试产物 —— 本项目只发功能件，这些既不随发布包分发也不上传
# ⚠️ _port_guard.py 是功能件（2/6 号 bat 依赖它），**不要**用 _*.py 通配排除
# ---------------------------------------------------------------------------
_audit.py
_audit*.py
_cmp_*.py
_concurrency_bench.py
_identify_flash.py
_opencode_*.py
_probe_*.py
_ratelimit_*.py
_speed_bench.py
_test_*.py
_verify_genclose.py
audit_out.txt
audit_proxy_fidelity.py
audit_*.py
compat_test.py
diag_compare.py
dsh_req.json
intel_results.json
probe_*.py
restart_proxy.ps1
s5.txt
test_e2e_mock.py
test_fixes.py
test_intelligence.py
verify_*.py
修思考链*.txt
压测总结_*.md
并发推理提示词.md
注入提示词.md
